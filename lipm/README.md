# To run the lipm example
Just run in your terminal:

```./lipm```

If a joystick is connected, use the left analog joystick to set the reference velocity for movement.

If no joystick is connected, use the keyboard: use ```WASD``` or the arrow keys to set the reference velocity, and 
```page up``` and ```page down``` to increase and decrease the velocity.
